import { config } from '../config';

export function helpText(): string {
  const p = config.botPrefix;
  return `👨‍👩‍👧‍👦 *בוט משפחתי - פקודות זמינות*

${p}שאל <שאלה> — שאל את Claude כל שאלה
${p}סיכום — סיכום השיחה של היום בקבוצה
${p}תזכורת <זמן> <טקסט> — קבע תזכורת
   דוגמאות: ${p}תזכורת 30 דקות להוציא אוכל
            ${p}תזכורת 18:00 לאסוף את יוני
${p}תזכורות — הצג תזכורות פעילות
${p}חידה — חידה לילדים (ענו כרגיל בקבוצה)
${p}עזרה — המסך הזה

💡 אפשר גם פשוט לכתוב @קלוד עם שאלה.`;
}
